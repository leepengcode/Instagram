class AppUrl {
  static var baseUrl = 'https://reqres.in';
  static var loginEndPoint = baseUrl + '/api/login';
  static var registerEndPoint = baseUrl + '/api/register';

  static const movieUrl = 'https://imdb-top-100-movies.p.rapidapi.com';

  // Add other endpoints here if needed
  static const topMoviesEndPoint = movieUrl + '/';
}
